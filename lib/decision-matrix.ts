import type {Decision} from "@/lib/engine";
export type DecisionMatrix={action:"LONG_WATCH"|"SHORT_WATCH"|"WAIT"|"PROCEED"|"REVIEW";gate:"PASS"|"CAUTION"|"BLOCK"|"NOT_APPLICABLE";reasons:string[];methodology:"deterministic-matrix-v2"};
export function buildDecisionMatrix(d:Decision):DecisionMatrix{const gate=d.riskGate??"NOT_APPLICABLE";const rec=d.recommendation.toLowerCase();const action=gate==="BLOCK"?"WAIT":gate!=="NOT_APPLICABLE"?(rec.includes("long")?"LONG_WATCH":rec.includes("short")?"SHORT_WATCH":"WAIT"):(d.priority==="HIGH"&&d.confidence>=.7?"PROCEED":d.confidence>=.5?"REVIEW":"WAIT");return{action,gate,reasons:["risk_gate="+gate,"confidence="+d.confidence.toFixed(3),"expectedR="+(d.expectedR??"UNAVAILABLE"),"mtf="+(d.multiTimeframe?.alignment??"UNAVAILABLE"),"conflict="+(d.signalConflict?.status??"UNAVAILABLE")],methodology:"deterministic-matrix-v2"}}
